package com.revature.main;

import java.time.LocalDate;
import java.util.ArrayList;

import com.revature.beans.Request;
import com.revature.beans.Upload;

public class Driver {

	public static void main(String[] args) {
		/* TESTING
		DaoHandler dh = new DaoHandler();
		for(Request r : dh.getRdi().getRequests())
		{
			System.out.println(r);
		}
		//dh.getEdi().addEmployee("Eric", "Pizo", "test@email", "pass", 0, 0, null, dh.getEdi().getEmployees());
		//dh.getRdi().addRequest(1000, LocalDate.now(), 1324.00, "Pending", "", "", 0, "", new ArrayList<Upload>(), dh.getRdi().getRequests());
		//dh.getUdi().addUpload("FileTest", 1000, dh.getUdi().getUploads());
		 */
	}
}
